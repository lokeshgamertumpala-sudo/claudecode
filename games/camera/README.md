# Camera Folder

Camera-related scripts and configurations:
- Camera controller (follow, zoom, shake)
- Camera bounds and limits
- Camera effects (screen shake, fade, flash)
- Camera manager for multiple viewports

Typical files:
- Camera.js
- CameraManager.js
- cameraConfig.json