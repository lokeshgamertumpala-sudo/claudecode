# SaveData Folder

Save and load system:
- SaveManager.js: handles localStorage persistence
- Save game versioning and migration
- Backup and cloud sync utilities (if implemented)

Save data includes player progress, currency, unlocks, settings, and statistics.