# Entities Folder

Game entity definitions:
- Unit.js: base unit class and specific unit types (Fast, Heavy, etc.)
- EnemyUnit.js: base enemy class and enemy variants
- EntityManager.js: object pooling and entity lifecycle management

Entities represent anything that exists in the game world with position, health, behavior, etc.