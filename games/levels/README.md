# Levels Folder

Level definitions and related data:
- JSON files describing each level layout
- Gate placements, enemy wave timings, obstacles
- Level metadata (name, difficulty, rewards, music)
- Possibly level scripts or triggers

LevelManager loads these files at runtime.